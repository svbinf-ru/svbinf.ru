import { MaterialsCode } from '../utils'

export const codeExamples: MaterialsCode[] = [
    {
        code: `def factorial(n):
    if n == 0:
        return 1
    return n * factorial(n - 1)

print(factorial(6))`,
        caption: 'Рекурсия',
        playground: 'https://www.online-python.com/Y2OINLUQuv',
    },
]
